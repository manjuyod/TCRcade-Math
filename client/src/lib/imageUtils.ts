/**
 * Images are intentionally disabled for question rendering in this build.
 */
export const getQuestionImage = (
  _questionText: string,
  _storyImage?: string | null,
): string | null => {
  return null;
};
